# Base path for subdirectory deployments (e.g., GitHub Pages)
# Leave empty for root domain deployments
BASE_PATH=/Baayno-Website

# SMTP server configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=username
SMTP_PASS=password
SMTP_FROM=no-reply@example.com
SMTP_TO=owner@example.com
